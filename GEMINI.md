# 프로젝트 작업 및 Git 브랜치 운용 규칙

이 프로젝트에서 작업할 때는 다음 규칙을 엄격히 준수합니다.

---

## 1. 🌿 Git 브랜치 운용 원칙 (main 직접 커밋 금지)

1. **`main` 브랜치 직접 커밋/푸시 절대 금지**:
   - `main` 브랜치는 프로덕션(Streamlit Community Cloud 배포) 브랜치이므로, 어떠한 경우에도 `main`에 직접 커밋하거나 푸시하지 않습니다.

2. **반드시 작업 브랜치 분기 후 개발**:
   - 새로운 기능 구현: `feat/<기능명>` (예: `feat/realtime-alert`)
   - 버그 및 오류 수정: `fix/<수정내용>` (예: `fix/db-connection`)
   - 리팩토링 및 개선: `refactor/<개선내용>` (예: `refactor/quant-screener`)
   - 모든 파일 수정, 코드 작성, 실험은 해당 작업 브랜치에서만 진행합니다.

3. **검증 및 사용자 승인 후 병합**:
   - 작업 브랜치에서 자동화 단위 테스트(`pytest tests/`) 및 구문 컴파일(`py_compile`)을 100% 통과해야 합니다.
   - 사용자에게 작업 내역과 검증 결과를 명확히 보고하고, 사용자의 명시적 병합(Merge) 승인이 있을 때만 `main`으로 병합하여 배포합니다.

---

## 2. 🔒 보안 및 시크릿 관리 원칙

1. **하드코딩 원천 금지**:
   - 관리자 비밀번호, API Key, DB 접속 정보 등 모든 민감 정보는 코드, 주석, 마크다운, 커밋 메시지에 절대로 평문으로 작성하지 않습니다.
   - 모든 시크릿은 `st.secrets` 또는 환경 변수(`os.environ`)를 통해서만 주입받습니다.

2. **Git 히스토리 보호**:
   - 실수로라도 민감 정보가 커밋 히스토리에 남지 않도록 항상 스테이징 전 `git diff`를 검토합니다.

---

## 3. 💬 소통 및 보고 원칙

1. **한국어 답변**: 모든 설명, 보고, 안내는 명확하고 친절한 한국어로 작성합니다.
2. **투명한 진행 보고**: 변경된 파일, 테스트 결과, Git 상태를 투명하게 공유합니다.
